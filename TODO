1. Create @Default decorator for class transformer (For default values)
2. Do smth with nest imports (For example - create function to aplly decorators for injects(Mb lib DI).)
3. Do smth with EntityClassOrSchema
